package de.hipp.constants;

public class EndpointConstants {

    private static final String baseUrl = "/api/";
    private static final String apiVersion = "v1/";
    public static final String CHARACTERENDPOINT = baseUrl + apiVersion + "character";
    public static final String RACEENDPOINT = baseUrl + apiVersion + "races";
}
